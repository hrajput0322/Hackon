<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Details</title>
    <!-- Bootstrap stylesheets -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap scripts -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <style>
        * {
            overflow-x: hidden;
            box-sizing: border-box;
            user-select: none;
        }

        .user-info {
            display: flex;
            align-items: center;
            padding: 10px 20px;
        }

        .user-info #login-button {
            margin-right: 10px;
            font-size: 20px;
        }

        .user-info .icons {
            display: flex;
            align-items: center;
        }

        .user-info .icons img {
            margin-right: 10px;
            width: 24px;
            height: 24px;
        }

        .header {
            background-color: #232f3e;
            color: #ffffff;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 10px 0;
        }

        .header .top-line {
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
            padding: 0 20px;
        }

        .header .brand {
            font-size: 36px;
            font-weight: bold;
            text-decoration: none;
            color: #ffffff;
            margin-right: 10px;
        }

        
        body {
            font-family: Arial, sans-serif;
            background-color: #f3f3f3;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        h1 {
            font-weight: bold;
        }

        .user-info-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 60vh; /* Adjust the height as needed */
            text-align: left;
        }

        .user-image img {
            max-width: 400px;
            max-height: 400px;
            border-radius: 50%;
            box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.5);
            margin: 10px;
        }

        .user-details {
            background-color: white;
            margin-left: 20px;
            padding: 10px;
            box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.5);
        }

        .user-details h2 {
            font-size: 28px;
            font-weight: bold;
        }

        .user-details p {
            font-size: 20px;
            color: #232f3e;
        }

        /* Add more styles as needed */

        /* Header and footer styles */
        header {
            background-color: #232f3e;
            color: #ffffff;
            text-align: center;
        }

        header h1 {
            font-size: 36px;
            font-weight: bold;
        }

        .text-container {
            width: 300px; /* Adjust the width as needed */
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .ellipsis {
            margin: 0;
        }

        .footer {
            background-color: #232f3e;
            color: #ffffff;
            text-align: center;
            padding: 20px 0;
            position: fixed;
            bottom: 0;
            width: 100vw;
        }

        .green-tick {
            color: #28A745; /* Green color for the tick */
            font-size: 24px; /* Adjust the size as needed */
            margin-right: 10px; /* Add spacing between the tick and text */
        }

        .black-tick {
            color: black; /* Green color for the tick */
            font-size: 24px; /* Adjust the size as needed */
            margin-right: 10px; /* Add spacing between the tick and text */
        }

        .no-display {
            display: none;
            font-size: large;
            color: green;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="top-line">
            <a class="brand" href="/"><img src="logo.png" alt=""> Amazon Re-Box</a>
            <h1 style="display: flex; align-items: center;"><img src="scan_page_icon.png" alt="" style="margin-right: 5px;">Green Streak</h1>
        </div>
    </div>
    <h1>Customer Details</h1>
    <div class="user-info-container">
        <div class="user-image">
            <img src="<%= user_image %>" alt="User Image">
        </div>
        <div class="user-details">
            <h2><%= username %></h2>
            <p><span class="black-tick">&#10004;</span>Address: <%= address %></p>
            <p><span class="black-tick">&#10004;</span>Phone Number: 8630875700</p></p>
            <p><span class="green-tick">&#10004;</span>Green Points: <%= green_points %></p>
            <button id="redeem-button" class="btn btn-success btn-lg" onclick="streakMaintained()">Streak Maintained?</button>
            <div class="no-display">
                <p>you gain 10 green points!</p>
            </div>
        </div>
    </div>
    
    <div class="footer">
        &copy; 2023 Amazon Re-Box
    </div>

    <script>
        function streakMaintained() {
            $.ajax({
                type: "POST",
                url: "/add-green-points",
                success: function (response) {
                    alert(`Customer gained 10 green points`);
                    location.reload();
                },
                error: function (xhr, status, error) {
                    console.log(xhr, status, error);
                }
            });
        }
    </script>
</body>
</html>